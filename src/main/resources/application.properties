spring.application.name=eazyschool
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# spring.security.user.name= <- set a custom username not apt for production
# spring.security.user.password= <- set a custom password not apt for production
# spring.thymeleaf.cache=false <- no longer needed: disabled by spring-boot-devtools
# debug=true <- print auto-configuration report on the console
# server.port=0 <- Random Port
# server.servlet.context-path=/justanindieguy
